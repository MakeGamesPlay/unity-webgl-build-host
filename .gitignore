# OS / editor cruft
.DS_Store
._*
Thumbs.db
desktop.ini
*.swp

# IDEs
.idea/
.vs/
.vscode/

# Release artifacts — attach these to the GitHub Release, don't track them.
*.unitypackage

# Go test artifacts (the shipped servers in Editor/HostBuild/Bin/ ARE committed)
Editor/HostBuild/Server~/*.test
Editor/HostBuild/Server~/*.test.exe

# NOTE: do NOT add a "*~" rule here — it would match the Server~/ source folder.
# NOTE: Unity .meta files are intentionally committed (required for UPM packages).
